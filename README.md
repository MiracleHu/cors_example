### CORS test

## run FE server
open node-http-frontend-server folder
run the command:
```sh
http-server -p 9999
```

## run BE server
open backend_server folder
run the command:
```sh
npm install
node index.js
```